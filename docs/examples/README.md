# Integration Examples

Примеры интеграции Eneca AI Bot в различные платформы и фреймворки.

## 📁 Файлы

### React (TypeScript)
**Файл:** [`react-chat.tsx`](react-chat.tsx)

Полнофункциональный компонент чата с:
- Streaming ответами (SSE)
- Стандартными ответами (REST)
- API Key аутентификацией
- Управлением conversation memory
- TypeScript типами

**Использование:**
```tsx
import EnecaChat from './react-chat';

function App() {
  return (
    <EnecaChat
      apiUrl="http://localhost:8000"
      apiKey="your_api_key"
      userId="user-123"
    />
  );
}
```

---

### Vanilla JavaScript
**Файл:** [`vanilla-chat.html`](vanilla-chat.html)

Самодостаточная HTML страница с чатом (без зависимостей):
- Два режима: Streaming и Standard
- Настраиваемый UI
- Конфигурационная панель
- Готова к использованию - просто откройте в браузере!

**Запуск:**
1. Запустите webhook сервер: `python webhook_server.py`
2. Откройте `vanilla-chat.html` в браузере
3. Настройте URL и API key (если нужно)
4. Начните чат!

---

## 🚀 Быстрый старт

### 1. Запустите сервер

```bash
# Активируйте виртуальное окружение
.venv\Scripts\activate  # Windows
source .venv/bin/activate  # Linux/Mac

# Запустите сервер
python server.py
```

### 2. Настройте API Key (опционально)

Отредактируйте `.env` в корне проекта:
```bash
API_KEY=your_secure_api_key_here
```

### 3. Протестируйте примеры

**Vanilla JS:**
- Откройте `vanilla-chat.html` в браузере
- Готово!

**React:**
- Скопируйте `react-chat.tsx` в ваш React проект
- Установите зависимости (если нужны)
- Импортируйте и используйте компонент

---

## 📚 Дополнительные ресурсы

- **Полное API руководство:** [`../API_INTEGRATION.md`](../API_INTEGRATION.md)
- **Webhook setup:** [`../README_WEBHOOK.md`](../README_WEBHOOK.md)
- **Быстрый старт:** [`../START_WEBHOOK.md`](../START_WEBHOOK.md)

---

## 💡 Примеры использования

### Python
```python
import requests

response = requests.post(
    "http://localhost:8000/api/chat",
    headers={"X-API-Key": "your_key"},
    json={"message": "Привет!"}
)
print(response.json()["response"])
```

### JavaScript (fetch)
```javascript
const response = await fetch('http://localhost:8000/api/chat', {
  method: 'POST',
  headers: {
    'Content-Type': 'application/json',
    'X-API-Key': 'your_key'
  },
  body: JSON.stringify({ message: 'Привет!' })
});

const data = await response.json();
console.log(data.response);
```

### cURL
```bash
curl -X POST http://localhost:8000/api/chat \
  -H "Content-Type: application/json" \
  -H "X-API-Key: your_key" \
  -d '{"message": "Привет!"}'
```

---

## 🔧 Настройка

### Streaming vs Standard mode

**Streaming (SSE):**
- ✅ Лучше для UX (мгновенная реакция)
- ✅ Подходит для длинных ответов
- ❌ Сложнее в реализации

**Standard (REST):**
- ✅ Проще в реализации
- ✅ Подходит для коротких ответов
- ❌ Пользователь ждет полного ответа

### Управление conversation memory

Сохраняйте `thread_id` между запросами для сохранения контекста:

```javascript
// Получить или создать thread_id
let threadId = localStorage.getItem('thread_id');
if (!threadId) {
  threadId = `web-${Date.now()}`;
  localStorage.setItem('thread_id', threadId);
}

// Использовать в запросах
fetch('/api/chat', {
  body: JSON.stringify({
    message: 'Hello',
    thread_id: threadId
  })
});
```

---

## 📝 TODO для адаптации под ваш проект

- [ ] Замените `apiUrl` на ваш production URL
- [ ] Добавьте ваш API key в конфигурацию
- [ ] Кастомизируйте UI под ваш дизайн
- [ ] Добавьте обработку ошибок под ваши нужды
- [ ] Настройте CORS если используете другой домен
- [ ] Добавьте analytics/логирование

---

## ❓ FAQ

**Q: Как открыть vanilla-chat.html?**
A: Просто дважды кликните на файл или откройте через браузер (File > Open).

**Q: Нужен ли npm/Node.js для vanilla примера?**
A: Нет! Vanilla пример работает без зависимостей.

**Q: Можно ли использовать React пример с JavaScript (не TypeScript)?**
A: Да, просто удалите типы или переименуйте в `.jsx`.

**Q: Поддерживается ли Vue/Angular/Svelte?**
A: Логика аналогична React примеру, адаптируйте под ваш фреймворк.

---

Удачной интеграции! 🚀
